# --- Core agent framework ---------------------------------------------------
deepagents>=0.6.0
langchain>=1.2.3
langchain-openai>=1.1.7
langchain-groq>=1.1.1
langchain-openrouter>=0.2.0      # OpenRouter provider (free + paid LLMs)
langchain-google-genai>=2.0.0    # Gemini provider (google_genai: model strings)
langchain-quickjs>=0.1.4

# --- Search ------------------------------------------------------------------
tavily-python>=0.7.17

# --- UI ------------------------------------------------------------------
streamlit>=1.58.0

# --- Persistence ---------------------------------------------------------
# (sqlite3 is in the Python standard library -- no package needed)

# --- Export --------------------------------------------------------------
reportlab>=4.2.0                  # PDF export (pure-Python, no system deps)

# --- Utilities -------------------------------------------------------------
python-dotenv>=1.2.1
pydantic>=2.7

# --- Notebooks (optional, only needed to run deepagentsdemo/*.ipynb) --------
ipykernel>=7.1.0
